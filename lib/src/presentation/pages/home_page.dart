import 'package:flutter/material.dart';

class HomePagr extends StatelessWidget {
  const HomePagr({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Center(
        child: Text(
          'Text',
        ),
      ),
    );
  }
}
